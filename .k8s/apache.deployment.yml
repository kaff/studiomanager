apiVersion: apps/v1
kind: Deployment
metadata:
  name: php-studio-manager-deployment
  labels:
    app: studio-manager-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: studio-manager-deployment
  template:
    metadata:
      labels:
        app: studio-manager-deployment
    spec:
      containers:
        - name: php-studio-manager
          image: studio-manager-ci:b1
          ports:
            - containerPort: 8080

